# Processed Data

`python -m ml.train` generates `processed_churn_sample.csv` in this directory.

The generated CSV is ignored by git because it can be recreated from the raw or
sample dataset and the preprocessing pipeline.
